[package]
name = "luna"
version = "0.1.0"
edition = "2024"

[dependencies]
ast_lowering = { path = "../ast_lowering" }
interface = { path = "../interface" }
lex = { path = "../lex" }
parse = { path = "../parse" }
